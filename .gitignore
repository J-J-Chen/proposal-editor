# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js / build
/.next/
/out/
/build
/dist
next-env.d.ts
*.tsbuildinfo

# env & secrets — NEVER commit the proxy token
.env
.env*.local
.env.local

# vercel
.vercel

# testing / coverage
/coverage

# merge-queue runtime state (scripts and .mq/check.sh ARE tracked)
/.mq/run/

# local caches (PDF parse cache, scratch)
/.cache/
/tmp/

# real client proposals + KB corpus — shared with permission; NEVER commit to the public repo
/proposals/
/kb/

# claude code — track project settings.json (team hooks), ignore personal local overrides
.claude/settings.local.json

# os / editor
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
.idea
.vscode/*
!.vscode/extensions.json
